# 과제 제출 v1.0.1

## 기능추가

- `fastapi-app/Dockerfile`을 추가해 FastAPI 앱을 컨테이너로 실행할 수 있도록 구성
- 루트 `docker-compose.yml`을 추가해 프로젝트 루트에서 통합 실행 가능하도록 구성

## 기능개선

- `todo.json`을 바인드 마운트해 컨테이너 재시작 후에도 할 일 데이터가 유지되도록 설정
- README에 프로젝트 구조와 Docker 실행 방법을 추가

## 결함수정

- 빌드 컨텍스트에 불필요한 캐시 파일이 포함되지 않도록 `.dockerignore` 추가
